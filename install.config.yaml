- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    #~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.screenrc: screenrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
